mod point;
mod polyomino;
// use point::Point;
// use polyomino::Polyomino;

fn main() {
    // let mut p = Point::new(1, 2);

    // p.shift(1, 1);
    // p.rotate_left(&Point::ORIGIN);
    // p.rotate_right(&Point::ORIGIN);
    // p.rotate_left(&Point::ORIGIN);
    // p.rotate_left(&Point::ORIGIN);
    // p.rotate_left(&Point::ORIGIN);
    // p.rotate_left(&Point::ORIGIN);

    // let _polyomino = Polyomino::from([p.clone(), p.clone()]);
    
    // println!("Point: {:?}", p);
}
